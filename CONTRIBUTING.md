# Contributing

Run `python -m pytest` before proposing a change. New behavior needs a failing test first, then the minimum implementation. Keep experiments reproducible: add configuration, seed handling, and machine-readable metrics. Do not present unmeasured performance changes as results.
